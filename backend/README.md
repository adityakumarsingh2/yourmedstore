# YourMedStore Backend

See the root [README.md](../README.md) for the complete assignment documentation, schema notes, unit conversion strategy, setup instructions, and deployment runbook.

Quick commands:

```bash
npm install
npm run db:migrate
npm run db:seed
npm run dev
```
